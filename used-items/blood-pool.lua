-- Left around to lure the vampire out of hiding
Blood_Pool = Class {
	init = function(self)

	end
}

function Blood_Pool:update(dt)
	if not self.active then
		return;
	end

end

function Blood_Pool:draw()
	if not self.active then
		return;
	end

end